# Codex

This dotfiles repo calls the shared layer **AI**.
Codex is a peer AI surface, not the only or primary tool.

Read:

- `~/.ai/README.md`
- `~/.ai/codex.md`
- `~/.ai/chatgpt.md` when macOS/iOS app integration matters
- `~/AI.md` for the root pointer

Keep this file small. Shared policy belongs in `~/.ai`, not here.
