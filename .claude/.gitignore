# Credentials and tokens
.claude.json
.claude.json.backup

# Conversation history (sensitive)
history.jsonl
projects/

# Personal data
file-history/
plans/
debug/

# Status cache — ignore runtime caches, but VERSION the per-host telemetry
# ledgers (git-crypt-encrypted via .gitattributes) for a private, cross-fleet history.
status/*
!status/telemetry-*.jsonl

# Misc
*.backup
security_warnings_state_*.json
